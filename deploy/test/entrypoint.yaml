---
apiVersion: v1
kind: Service
metadata:
  name: entrypoint
  annotations:
    metallb.io/address-pool: lan-pool
    metallb.io/loadBalancerIPs: 192.168.2.204
  labels:
    app.kubernetes.io/component: entrypoint
    app.kubernetes.io/part-of: mealplanner-test
spec:
  type: LoadBalancer
  selector:
    app: entrypoint
  ports:
    - name: web
      port: 80
      targetPort: web
      protocol: TCP
    - name: websecure
      port: 443
      targetPort: websecure
      protocol: TCP
